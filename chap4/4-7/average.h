#ifndef GUARD_average
#define GUARD_average

// average.h header file
#include <vector>
#include <iostream>

// calculate the average of a vector of doubles
double average_of_vector(const std::vector<double>&);
std::istream& read_numbs(std::istream&, std::vector<double>&);


#endif